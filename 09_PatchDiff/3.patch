--- main2.c	2025-12-21 22:16:09
+++ main3.c	2025-12-21 22:16:31
@@ -3,7 +3,7 @@
 #include <time.h>
 #include <string.h>
 
-#define N (argc > 2 ? atoi(argv[2]) : 6)
+#define N (argc > 3 ? atoi(argv[3]) : 6)
 
 void gen(int h, int w, char maze[h][w]) {
     for (int i = 0; i < h; i++) 
@@ -52,12 +52,13 @@
 }
 
 int main(int argc, char *argv[]) {
-    srand(time(NULL));
+    int seed = argc > 1 ? atoi(argv[1]) : time(NULL);
+    srand(seed);
     
     char pass = '.', wall = '#';
-    if (argc > 1 && strlen(argv[1]) >= 2) {
-        pass = argv[1][0];
-        wall = argv[1][1];
+    if (argc > 2 && strlen(argv[2]) >= 2) {
+        pass = argv[2][0];
+        wall = argv[2][1];
     }
     
     int h = 2*N+1, w = 2*N+1;
